

function expire(req, res, next) {
    /*
    Search for user then access "expire" property, checking if it
    equals "date.now". If it does, return 401 status code
    (unauthorized access)
    */

}